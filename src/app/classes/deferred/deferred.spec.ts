/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Deffered} from './deffered';

describe('Deffered', () => {
  it('should create an instance', () => {
    expect(new Deffered()).toBeTruthy();
  });
});
