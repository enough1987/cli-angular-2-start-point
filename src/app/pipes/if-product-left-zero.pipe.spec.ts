/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IfProductLeftZeroPipe } from './if-product-left-zero.pipe';

describe('Pipe: IfProductLeftZero', () => {
  it('create an instance', () => {
    let pipe = new IfProductLeftZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
